# Aether 全量修复执行计划（Wave 0-6，覆盖全部 253 项）

- 依据：docs/AUDIT-2026-08-29-round1.md（253 项：P0×47 / P1×86 / P2×79 / P3×41）
- 本计划在用户提供的 Plan Agent「Wave 0-2」基础上补全 Wave 3-6，并修正 2 处并行文件冲突：
  - 冲突 1：W1-T3 与 W1-T10 同改 workflows/index.ts → 合并到同一执行组
  - 冲突 2：W1-T5 与 W1-T9 同改 media/index.ts → 合并到同一执行组
  - 冲突 3：W2 中 conversations/index.ts 被 T16/T17/T20/T21/T22/T23 触及 → 合并单一执行组
- 基线门禁（每个 Wave 后必过）：typecheck PASS / lint 0 error / build PASS / test 全 PASS（backend 改动先 build:backend 再 test:backend）
- 并行纪律：同一代码库内并行代理只做 `tsc --noEmit` 验证；dist 构建与测试由编排方在 Wave 门禁统一执行（避免 dist 写竞争）

---

## Wave 0 构建链（2 项）— ✅ 已完成并验证
| 任务 | 状态 | 验证 |
|---|---|---|
| W0-T1 test 前自动 build shared+backend | ✅ | clean 后 npm test：shared 9/9、backend 82/85（0 fail）、frontend 9/9 |
| W0-T2 clean 跨平台 + tsbuildinfo 同清（修复增量编译陷阱） | ✅ | npm run clean 后 dist 全删、Windows 无错 |

## Wave 1 安全 P0（13 项）— 5 路并行（文件互不相交）
| 组 | 文件 | 任务 | 验证 |
|---|---|---|---|
| W1-A | lib/command.ts, modules/data/index.ts, modules/workflows/index.ts, modules/providers/index.ts | SEC-001/002/003：cmd.exe 整串 → spawn argv {shell:false} + 内建命令原生化（dir/type/echo/pwd/cd）；SEC-010：provider.baseUrl 创建/更新/执行前三处 isSafeFetchUrl 校验 | tsc --noEmit；门禁 curl `dir & whoami` 阻断 |
| W1-B | modules/documents/index.ts, modules/media/index.ts, modules/export/index.ts | SEC-004：不信 DB doc.path，UUID 文件名运行时重建；SEC-005/006：`startsWith(resolve(dir)+sep)` 替换失效 relative() 校验；SEC-009：media 5 处 provider 返回 URL fetch 前 isSafeFetchUrl | tsc --noEmit；门禁 curl `../../etc/passwd` → 403 |
| W1-C | modules/search/index.ts, modules/mcp/index.ts | SEC-007：fetch redirect:'manual' + 逐跳 isSafeFetchUrl（≤5 跳）；SEC-008：mcp server.url 创建/更新/测试三处校验 | tsc --noEmit；门禁 169.254.169.254 → 400 |
| W1-D | config/index.ts, lib/crypto.ts | SEC-011：密钥落盘 → Windows DPAPI（优先复用已装 koffi 调 CryptProtectData，非 Windows 降级文件 0600）；SEC-012：decrypt 拒绝非 enc: 明文 + 存量迁移脚本 | tsc --noEmit；门禁明文 key 抛错、迁移后旧 key 可用 |
| W1-E | src/mobile/src/api/supabase.ts | MOB-01：移除 localStorage 存 service_role key；改经后端 /api/sync/* 代理（Option B，桌面端已持有配置）；保留 URL 存储 | mobile vite build PASS；localStorage 无 key |

**Wave 1 门禁**：typecheck + lint + build:backend + test:backend + build:frontend + test:frontend 全 PASS + 启动后端 curl 三项注入/穿越/SSRF 手工验证

## Wave 2 逻辑/协议/性能 P0（27 项）— 9 路并行
| 组 | 文件 | 任务 |
|---|---|---|
| W2-F | api/streamClient.ts | ST-01：envelope 分支发射后显式 return，消除双渲染 |
| W2-G | components/Layout.tsx | FE-01 远程命令轮询集中 Layout（CodingHome 改事件监听）；FE-02 轮询 fetch AbortController；FE-03 幻灯片 timer 完整依赖+函数式 cleanup |
| W2-H | routes/Chat.tsx | FE-04 事件轮询 loadReqIdRef 序号守卫；FE-05 每次发送新建 AbortController（旧者独立清理）；FE-07 合并 mount 双加载（ref 守卫） |
| W2-I | routes/CodingHome.tsx | FE-06 合并 mount 双加载；FE-08 消息 1s 轮询序号守卫+按 ID 合并 |
| W2-J | routes/Media.tsx, routes/Providers.tsx, routes/Settings.tsx | FE-09 删假进度条改真实耗时；FE-10 getSupabase 显式 throw/真实 client；FE-11 key 解析完成前不置 connected |
| W2-K | app.ts, db/client.ts, lib/tool-executor.ts, modules/data/index.ts | BE-01 markDirty 双 timer 同清+幂等；BE-02 executeFileTool 补 await（tool-executor:113 + workflows:92——workflows 文件已由 W1-A 改完，此处仅补 await）；BE-06 导入 messages 移入事务 |
| W2-L | modules/sync/index.ts | BE-03 轮询/重连 try/catch 错误边界；BE-07 会话+首消息补偿重试（幂等键 commandId） |
| W2-M | modules/conversations/index.ts, modules/agents/index.ts, db/schema, db/migrate.ts | BE-04 AI 非 ok 非可重试显式抛错；BE-05 clientAbort.signal 传入 executeTool 前后检查；BE-08 heartbeat try/finally；PF-01 conversations 加 tokenTotal 列（迁移 v9+回填）移除 N+1 全扫；PF-02 maxToolCallsPerRequest=50 预算；PF-03 429 熔断+jitter+Retry-After |
| W2-N | store/activityStore.ts | PF-04 去 flushSync 改 React 19 批处理；PF-05 projectTaskCard 增量投影（缓存 lastSeq） |

**Wave 2 门禁**：全量门禁 + 启动冒烟（后端无未捕获异常、前端可访问）

## Wave 3 God 文件拆分（P0 结构 14 项）— 依赖 Wave 2（同文件稳定后）
纪律：先跑现有 85 测试作特征网 → 拆分 → 测试保持全绿；每次拆分一个文件即门禁一次
| 序 | 目标 | 拆分方向 |
|---|---|---|
| W3-1 | lib/command.ts, lib/search-tools.ts, lib/event-bus.ts, lib/dal.ts（GF-07..10） | executor/validator/history；工具分域；chunk-packer/persistence；按实体 |
| W3-2 | modules/conversations/index.ts（GF-02，1800 行） | chat-handler/sse-stream/tool-loop/compaction/routes |
| W3-3 | modules/agents/index.ts（GF-01，2300 行） | agent-definitions/orchestration/sse-handler/tool-loop/routes |
| W3-4 | modules/sync/index.ts（GF-03，2000 行） | sync-config/realtime/polling/command-processor/engine |
| W3-5 | modules/toolbox + workflows + data（GF-04/05/06） | 按域；store/engine/executors/ai-creator；settings/projects/import-export/exec |
| W3-6 | 前端 CodingHome+Chat（GF-11 + D02/D03/D04 重复归并） | hooks/useConversations、useStreamSend、useMessagePolling |
| W3-7 | Media.tsx + Settings.tsx（GF-12/13） | Form/Gallery/Preview；Sync/Appearance/General |
| W3-8 | Workflows/Toolbox/Knowledge/Layout（GF-14） | 按域拆子组件 |

## Wave 4 P1（86 项）— 批量
| 组 | 内容 |
|---|---|
| W4-1 | 认证层：敏感端点（terminal/security 设置/key 读取）localhost token；Swagger 生产关闭；CORS 配置化（SEC-016..020, SEC-021/022 Electron https/端口） |
| W4-2 | 重复逻辑归并：lib/force-summary、lib/deduplicate、lib/ai-analysis、lib/ai-fallback、lib/provider-test（CQ-015..020 全部调用点切换） |
| W4-3 | lib/path-guard.ts 单一化（CQ-019 的 4 变体统一，全模块强制引用） |
| W4-4 | lib/fetch-retry.ts 落地 + 5 模块改引用（CQ-048..052 消循环依赖） |
| W4-5 | UI Token：删 base.css OKLCH 覆盖、@theme 映射主题变量、按钮三合一（删 hover-glow 与 .btn-primary）、on-accent 前景语义化、index.html 预置 data-theme 防闪烁（UI-01..04） |
| W4-6 | 共享组件：Tabs/Input/Textarea/EmptyState/Spinner/Modal；50+ 内联卡片归并 .glass-card；Toolbox 原生 confirm 替换 |
| W4-7 | a11y：focus-visible 全覆盖、aria-label、tab 方向键、live region、模态焦点陷阱、skip link |
| W4-8 | 响应式：CommandCenter/Knowledge/Settings 网格移动断点、Chat 定宽改弹性、Projects 行高 |
| W4-9 | 功能补全：/api/export 与 export/all 统一；搜索排序+分页；记忆自动抽取调度 |
| W4-10 | 工作流：并行分支执行 + 环检测快速失败 |
| W4-11 | 工具箱收尾：完成 README 🚧 项或下架入口 |
| W4-12 | 后端测试补盲：providers/conversations/agents/data/media/mcp/terminal/documents 核心模块 |
| W4-13 | 前端测试补盲：Chat/Settings/Providers/activityStore 关键路径 |
| W4-14 | 性能 P1：MCP 工具移出 loop、sync 批量 upsert、工具箱重资源全懒加载、图转 PDF 流式、monitoring TTL 缓存 |

## Wave 5 P2（79 项）— 批量
| 组 | 内容 |
|---|---|
| W5-1 | magic 常量化：轮询间隔/事件名常量文件、AI 参数入 config、timeout 统一 |
| W5-2 | 列表端点分页：conversations/agents/workflows/documents/media |
| W5-3 | EventBus seq 启动一次初始化；overflowHistory 上限；字符→token 估算改 tokenizer 或分语言系数 |
| W5-4 | 静默 catch 日志化（pino 结构化）+ console.* 全替换 |
| W5-5 | mobile 纳入 workspaces + tsconfig 对齐 + 与前端共享聊天组件 |
| W5-6 | 工程：图标脚本三合一、杀端口统一 build/kill-port.js、start.bat 走 npm start、backend workspace 补依赖声明、.gitignore/.env.example 补全、eslint no-explicit-any 升 error 分阶段 |
| W5-7 | UI：动画常量统一（stagger/lift/scale/modal）、4 种圆角归 token、fontSize px 全 token 化、滚动条全局、亮主题重复定义合并 |
| W5-8 | 安全 P2：import/all Zod+消毒、testing/run 限流、monitoring 收敛、export 行数上限 |

## Wave 6 P3（41 项）+ 卫生
| 组 | 内容 |
|---|---|
| W6-1 | 死代码：hover-glow-button（随 W4-5）、wechat-bridge 空目录、dist_exe_old_102205、/api/chat 废弃端点、注释代码块、未使用导出 |
| W6-2 | 文档：AUDIT.md 状态对齐、README 状态徽标、PLAN.md 同步、归档失效研究文档 |
| W6-3 | 数据卫生：settings.json 3MB base64 背景图外移 data/backgrounds、旧产物密钥副本清除 |
| W6-4 | 安全 P3：CSP connect-src 收敛具体端口、Electron connect-src 域名白名单、crypto 日志脱敏、MCP allowlist 收紧（去 deno/bun 远程拉脚本面）、stdio maxBuffer、workflow 去 start/explorer |
| W6-5 | 杂项：shared subpath exports、baseUrl schema 修正、second-instance argv、shutdown IPC 优雅 flush、快速启动.bat 改名 |

## 终审循环
1. 第二轮全量复审：6 路代理重扫全项目（与第一轮同维度）→ 新审计报告对比
2. 新 P0/P1 继续修复 → 再复审 → 直至退出条件（无新 P0/P1、核心流程全过、门禁全绿）
3. 最终验收报告（评分/修复统计/重构统计/验证结果/剩余问题）→ Oracle 验证
